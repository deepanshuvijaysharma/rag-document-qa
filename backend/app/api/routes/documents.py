"""Document Management API Endpoints (Skeleton)."""

try:
    from fastapi import APIRouter

    router = APIRouter()

    # Document management routes (upload, list, delete) will be implemented in subsequent phases.
except ImportError:
    router = None  # type: ignore
